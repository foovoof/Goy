# قواعد التسمية
version: 1.0.0
rules:
  - id: snake-case-fields
    pattern: '^[a-z][a-z0-9_]*$'
    scope: property
  - id: pascal-case-titles
    pattern: '^[A-Z][A-Za-z0-9]+$'
    scope: schema.title
  - id: kebab-case-files
    pattern: '^[a-z][a-z0-9-]*\.schema\.json$'
    scope: filename
