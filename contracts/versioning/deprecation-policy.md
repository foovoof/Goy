# سياسة الإهمال

- يمر أي عقد قبل الحذف بمرحلة `deprecated` لا تقل عن دورتي إصدار.
- توضع علامة `deprecated: true` في المخطط ورسالة `x-deprecation-note` في OpenAPI.
- يتم إعلام المستهلكين عبر قناة `#contracts-announce` قبل 30 يومًا من الإزالة.
