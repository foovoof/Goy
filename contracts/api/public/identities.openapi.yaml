openapi: 3.1.0
info:
  title: Identities API (Public)
  version: 1.0.0
  description: إدارة الهويات — واجهة عامة
servers:
  - url: https://api.goy.local/v1
tags:
  - name: Identities
paths:
  /identities:
    get:
      tags: [Identities]
      summary: قائمة identities
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200':
          description: قائمة العناصر
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/Identities' }
                  page: { type: integer }
                  total: { type: integer }
    post:
      tags: [Identities]
      summary: إنشاء identitie
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/Identities' }
      responses:
        '201':
          description: تم الإنشاء
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Identities' }
  /identities/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [Identities]
      summary: قراءة عنصر
      responses:
        '200':
          description: العنصر
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Identities' }
        '404': { description: غير موجود }
    patch:
      tags: [Identities]
      summary: تحديث جزئي
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/Identities' }
      responses:
        '200':
          description: تم التحديث
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Identities' }
    delete:
      tags: [Identities]
      summary: حذف
      responses:
        '204': { description: تم الحذف }
components:
  parameters:
    Page:
      name: page
      in: query
      schema: { type: integer, minimum: 1, default: 1 }
    PageSize:
      name: page_size
      in: query
      schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
  schemas:
    Identities:
      type: object
      required: [id]
      properties:
        id: { type: string, format: uuid }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        name: { type: string }
        metadata: { type: object, additionalProperties: true }
